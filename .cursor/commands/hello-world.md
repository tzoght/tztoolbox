# Hello World

A minimal Cursor command. Replace this with your own prompt or workflow.

## What to do

When the user invokes this command (via `/` in Cursor), the agent will follow the instructions below.

- Say hello and confirm the command ran.
- Optionally ask what the user would like to do next.

Replace this file's content with your real command: the first heading is the name shown in the command palette; the rest is the instruction body for the agent.
