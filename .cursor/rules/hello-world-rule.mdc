---
description: Minimal rule that applies when no other rule matches. Use as a template for new rules.
globs: 
alwaysApply: true
---

# Hello World Rule

A minimal Cursor rule. Rules in `.cursor/rules/` give the agent persistent context for this project.

- Prefer clear, readable code and concise comments.
- When in doubt, ask the user rather than assume.

Replace this file's content with your real rule. Use `globs` for file-specific rules (e.g. `**/*.ts`) or `alwaysApply: true` for project-wide guidance.
